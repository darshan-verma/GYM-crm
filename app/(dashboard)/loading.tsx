import PageLoading from "@/components/shared/PageLoading"

/**
 * Loading UI for all dashboard routes.
 * Shown in the main content area while the page loads (sidebar and header remain).
 */
export default function DashboardLoading() {
  return <PageLoading />
}
